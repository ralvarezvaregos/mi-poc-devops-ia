name: Mi PoC de DevOps
on: [push]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Saludo de IA
        run: echo "¡La automatización está viva!"
