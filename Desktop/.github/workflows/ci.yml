name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Linter de IA ejecutado
        run: echo "Linter de IA ejecutado"
        
      - name: Saludo personalizado
        run: echo "¡Hola Rodrigo! Bienvenido a tu pipeline de CI/CD"